# Copy to keystore.properties (git-ignored) to sign release builds with your own key.
# Without this file, release builds fall back to the debug key so anyone can still build.
storeFile=/absolute/path/to/beryl-release.jks
storePassword=...
keyAlias=beryl
keyPassword=...
